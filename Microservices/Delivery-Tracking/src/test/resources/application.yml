spring:
    application:
      name: delivery-tracking
    jpa:
      hibernate:
        ddl-auto: create-drop # vai apagar o banco toda a vez que inicializar novamente os testes de integração
      show-sql: true
    datasource:
      url: jdbc:postgresql://localhost:5432/deliverydb_test
      username: postgres
      password: postgres
